JsonBig = Json (

    benchmark = (
      ^ (JsonParser with: self class AllSets) parse.
    )
    
    verifyResult: result = (
      ^true
    )
    
    ----
    
    AllSets = (
      ^ ''
    )
)