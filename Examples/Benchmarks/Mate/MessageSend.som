MessageSend = Benchmark (
    | object |
    
    initialize = (
    	object := Mock new.
    )
    
    benchmark = (
    	^object key.
    )
    
    verifyResult: result = (
      ^self assert: result equals: 1
    )
)    
